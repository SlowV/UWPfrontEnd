<Page x:ConnectionId='1'
    x:Class="SlowVMusic.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlowVMusic.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:SlowVMusic.Entity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Pivot>
        <!--<Pivot.RightHeader>
            <CommandBar>
                <AppBarButton Icon="List" Background="Transparent"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Add" Background="Transparent"/>
            </CommandBar>
        </Pivot.RightHeader>-->
        <PivotItem Header="List Song">
            <ScrollViewer>
                <ListView x:ConnectionId='2'                                             >
                    <ListView.ItemTemplate>
                        <DataTemplate                         >
                            <StackPanel x:ConnectionId='3' Orientation="Horizontal">
                                <Image x:ConnectionId='4'                             Width="60" Height="60"/>
                                <TextBlock x:ConnectionId='5'                      HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </PivotItem>
        <PivotItem Header="List Song">
            <Grid>
                <StackPanel Margin="50,20">
                    <TextBox PlaceholderText="Nhập tên bài hát" Header="Tên bài hát"/>
                    <TextBox PlaceholderText="Ảnh bài hát" Header="Ảnh" Margin="0,10,0,0"/>
                    <Button Content="Thêm"  Margin="150,10" Style="{Binding Source={StaticResource myButtonPrimary}}"/>
                </StackPanel>
            </Grid>
        </PivotItem>
    </Pivot>
    
</Page>

